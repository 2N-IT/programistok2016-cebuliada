var answerId = <%= @answer.id %>
var currentScore = $('.current-score')
var scope = $('#answer_' + answerId)
$('a', scope).addClass('disabled')
currentScore.text(parseInt(currentScore.text()) + parseInt(<%= @points %>))
